# Automata visualization

Exports NFA, DFA, and MinDFA DOT files plus CSV statistics. MinDFA should preserve accepted token rules while reducing equivalent DFA states; exact counts depend on the baseline rule set.
