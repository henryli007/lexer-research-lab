# Code-driven DFA

Prototype benchmark wrapper for a future switch/goto DFA encoding. The current implementation delegates tokenization to the baseline while preserving the measurement surface and generated-code hook, so performance results should not yet be interpreted as a real encoding comparison.
