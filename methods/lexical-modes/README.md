# Lexical modes

Prototype lexer demonstrating mode transitions for comments, strings, and template text/expression regions. Run `./build/mode_lexer_cli methods/lexical-modes/examples/template_demo.txt --template`. It complements the baseline by making context explicit; it is not a replacement C-- lexer.
