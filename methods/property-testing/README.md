# Property-based testing

The fuzzer generates legal and malformed snippets and checks: no infinite loop, monotonic token positions, longest operator match, invisible comments, keyword priority, error locality, and round-trip token-type stability.
